<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/head'); -%>
</head>

<body>
    <header>
        <img class="logo" src="images/logo.svg" alt="Logo">
        <%- include('partials/nav'); -%>
    </header>
    <main>
        <ul>
            <% if (locals.data && locals.data.length > 0  ){ %>
                <% for (let i = 0; i < data.length; i++) { %>
                    <li>
                        <figure class="profiel">
                            <img class="profielfoto" src="images/profielfotos/<%= data[i].photo %>" alt="Profilepicture">
                            <div class="knoppen">
                                <a id="dislikelink" href="<%= data[i].email%>"><img src="images/no.png" alt="DislikeButton"></a>
                                <form action="<%= data[i].email %>" method="post">
                                    <button class="likebut" type="submit"> <img src="images/matches.svg" alt="LikeButton"> </button>
                                </form>
                            </div>
                            <figcaption>
                                <div>
                                    <h2>
                                        <%= data[i].voornaam + ' ' + data[i].achternaam %>
                                    </h2>
                                    <p class="bio">
                                        <%= data[i].bio %>
                                    </p>
                                </div>
                            </figcaption>
                        </figure>
                    </li>
                    <% } %>
                        <% } else { %>
                            <p>Er zijn geen gebruikers meer over. U heeft iedereen gehad</p>
                            <% } %>
        </ul>

    </main>

    <script src="./js/swipen.js"></script>
</body>

</html>